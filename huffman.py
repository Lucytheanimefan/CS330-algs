arr = ['a', 'b', 'c', 'd', 'e', 'f'];
freq = [5, 9, 12, 13, 16, 45];

class Node:
	def __init__(self, letter, freq):
		self.letter = letter
		self.freq = freq
		self.left = None
		self.right = None

	def __str__(self): 
		return letter + ": "+freq



class HuffmanTree:
	def __init__(self):
		self.root = None

	def insert(node):
		

